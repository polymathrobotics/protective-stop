.DS_Store
__pycache__
.pytest_cache
node_modules
/.ruff.toml
*/build/
*/build-cov/
*.cov

# ESP-IDF firmware build artifacts
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/sdkconfig.credentials
# host runner binary
host/machine_app_runner
# host unit-test binaries
host/test_clock_guard
host/test_crypto_kat
host/test_demote_veto
host/test_aux_channel
# cppcheck
*.ctu-info
cppcheck-build/
firmware/managed_components/

# Production flash images (remote + machine roles) — contain baked-in secrets
# (Tailscale key, WiFi creds, admin password, fleet key). Never commit.
tools/production_image/
tools/production_image_machn/

# analysis / CAD / stray artifacts
*.dump
*.pyc
*.FCBak
nohup.out
machn/build/
machn/sdkconfig
machn/sdkconfig.credentials
machn/sdkconfig.old
machn/managed_components/

# LOCAL build-source plugin for flash_station.py (fleet client + creds).
# Nextcloud-only, never committed.
tools/app_source.py

# coverage artifacts
*.gcda
*.gcno
*.gcov
docs/safety/coverage/*.html
docs/safety/coverage/*.css
ros2/build-cov/
ros2/install-cov/
ros2/log/
firmware/test/test_estop_verdict
firmware/test/coverage-summary.json

build/
install/
log/
tools/fleet.env
